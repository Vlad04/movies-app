export interface Director {
    pkDirector: number;
    name: string;
    age: number;
    active: boolean;
}

export interface SaveDirector {
    name: string;
    age: number;
    active: boolean;
}